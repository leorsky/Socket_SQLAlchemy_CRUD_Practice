[project]
name = "project-new"
version = "0.1.0"
description = ""
authors = [
    {name = "LeoR",email = "leorsky77@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[tool.poetry]
packages = [{include = "project_new", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
